#!/bin/zsh
#
# .aliases - Set whatever shell aliases you want.
#

# Basic commands replacement
alias cd=z
alias cdi=zi

alias http=xh

alias fdf='fd -type f'
alias fdd='fd -type -d'

alias grep=rg

alias cat=bat

alias ls=eza
alias ll='eza -l'
alias la='eza -lA'

alias jq=jaq

# Tar
alias tarls="tar -tvf"
alias untar="tar -xf"