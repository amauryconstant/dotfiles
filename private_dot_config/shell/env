#!/usr/bin/env sh

# We need to set $ENV so that if you use shell X as your login shell,
# and then start "sh" as a non-login interactive shell the startup scripts will
# correctly run.
export ENV="$HOME/.config/shell/interactive"

# We also need to set BASH_ENV, which is run for *non-interactive* shells.
# (unlike $ENV, which is for interactive shells)
export BASH_ENV="$HOME/.config/bash/env"

# Define scripts directory for all shell scripts
export SCRIPTS_DIR="$HOME/.local/lib/scripts"
export UI_LIB="$SCRIPTS_DIR/core/gum-ui.sh"

# XDG-compliant Claude config directory
export CLAUDE_CONFIG_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/claude"

# UI library is now lazy-loaded on demand - no longer sourced at startup

umask 0077