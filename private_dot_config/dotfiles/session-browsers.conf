# Browsers with built-in session restoration
# Only the first window (lowest workspace ID) is saved for these apps.
# The browser itself restores remaining windows/tabs via its own dialog.
# Format: One class name per line (case-insensitive)

firefox
chromium
brave
vivaldi
opera
