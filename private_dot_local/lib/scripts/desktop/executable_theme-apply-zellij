#!/usr/bin/env sh
# Script: theme-apply-zellij
# Purpose: Signal running zellij sessions to reload config after theme change

if pgrep -x zellij >/dev/null 2>&1; then
    touch "$HOME/.config/zellij/config.kdl" 2>/dev/null
fi
