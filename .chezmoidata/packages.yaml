packages:
  install:
    arch:
      # Fonts
      - ttf-firacode-nerd otf-opendyslexic-nerd otf-geist-mono-nerd ttf-fira-sans
      # Terminal essentials
      - zoxide fzf tlrc xh fd ripgrep broot git-delta bat eza fastfetch jaq rsync
      # Terminal presentation
      - vhs
      # Package version managers
      - uv nvm cargo
      # Development tools
      - docker docker-compose aws-cli code
      # Software package systems
      - appimagelauncher snapd
      # General software
      - firefox chromium spotify nextcloud-client qbittorrent
  delete:
    arch: tldr