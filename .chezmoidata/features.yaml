# Optional feature toggles
# Set enabled: false to skip setup without removing the package from packages.yaml

features:
  kanata:
    enabled: true
  voxtype:
    enabled: true
    gpu_device: 0
  restic:
    enabled: true
