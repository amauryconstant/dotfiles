IdentitiesOnly yes

# Global defaults — options before the first Host block apply to every host.
# Surface a dropped connection in ~45s instead of waiting for TCP to give up,
# which is what leaves a terminal stuck in the alternate screen.
ServerAliveInterval 15
ServerAliveCountMax 3

# GITHUB - Personal
Host github.com
   HostName github.com
   PreferredAuthentications publickey
   IdentityFile ~/.ssh/github

# GITLAB - Personal
Host gitlab.com
   HostName gitlab.com
   PreferredAuthentications publickey
   IdentityFile ~/.ssh/gitlab

Match host ovh-vps exec "ping -c1 -W1 100.64.0.1"
  HostName 100.64.0.1

Match host ovh-ks5 exec "ping -c1 -W1 100.64.0.2"
  HostName 100.64.0.2

# OVH Server - VPS
Host ovh-vps
  HostName {{ .ovhVPSServerIP }}
  User amaury
  PreferredAuthentications publickey
  IdentityFile ~/.ssh/ovh-vps

# OVH Server - Bare Metal KS5
Host ovh-ks5
  HostName {{ .ovhKS5ServerIP }}
  User amaury
  PreferredAuthentications publickey
  IdentityFile ~/.ssh/ovh-ks5

