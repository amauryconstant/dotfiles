#!/bin/sh

{{ if eq .osId "linux-arch" }}

    rbw config set email {{ .personalEmail }}
    rbw config set base_url {{ .vault }}
    rbw login
    rbw sync
    rbw get dotfiles-key > $HOME/.keys/dotfiles-key.txt
    rbw lock

{{ end }}
